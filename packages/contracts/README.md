# @portfolio/contracts

Typed transport contracts live here.

This package will contain request/response schemas shared by the web client and API transport. It must express application-facing contracts, not database rows and not Supabase generated table types.

No contract has been introduced yet because Phase 0 deliberately stops before exposing the first HTTP endpoint.
